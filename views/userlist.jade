extends layout

block content
    h1.
        Administrator Editing Page    
    ul
        each user, i in userlist
            li.mybox 
                p Post Title: #{user.postname}
                p Date: #{user.date}
                p Name: #{user.username}
                a(href="/singleview/#{user.id}") View
                =" "
                a(href="/edituser/#{user.id}") Edit
                =" "
                a(href="/deleteuser/#{user.id}", onclick="return deleteWarning();" ) Delete
    script.
        function deleteWarning() {
        if (confirm("Are you sure you want to delete?") == true)
        {return true;
        console.log("item deleted")}
        else
        {console.log("delete cancelled")
        return false; }
        }
